{% extends 'base.html.twig' %}

{% block body %}
    <h1>Phylum</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ phylum.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ phylum.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('phylum_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('phylum_edit', { 'id': phylum.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
